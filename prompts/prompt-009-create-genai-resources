create the Azure resources needed for a generative AI chat like user interface including any new azure resource IaC, 
endpoints, keys etc but try to use managed identity where possible 

use low cost dev SKUs where possible and use GPT-3.5-Turbo in UKSOUTH

store the settings for these new resources in a GenAISettings file
